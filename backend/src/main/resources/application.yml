server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL:jdbc:sqlite:data/geosmart_main.sqlite?busy_timeout=15000}
    username: ${DB_USER:sa}
    password: ${DB_PASSWORD:sa}
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1
      minimum-idle: 1
      connection-timeout: 30000
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GEOSMART_MAIL_USERNAME:badagaclass@gmail.com}
    password: ${GEOSMART_MAIL_APP_PASSWORD:urfjhbtwlirbznsm}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
        jdbc:
          lob:
            non_contextual_creation: true
  flyway:
    enabled: true

app:
  jwt:
    secret: "geosmartmanager-secret-key-please-change-32bytes"
    expiration-ms: 86400000
  auth:
    password-reset-url-base: ${APP_AUTH_PASSWORD_RESET_URL_BASE:http://localhost:5173/reset-password}
    password-reset-token-expiration-minutes: ${APP_AUTH_PASSWORD_RESET_TOKEN_EXPIRATION_MINUTES:30}
  compliance:
    min-parcel-area-sqm: 150
    max-parcel-area-sqm: 100000
    max-parcel-count: 200
    max-parcel-aspect-ratio: 6
    framework-version: "RLMUA Subdivision Framework 2026.1"
    updates:
      - id: "RLMUA-2026-01"
        title: "Subdivision evidence package tightened"
        clause-reference: "RLMUA-LS 8.4"
        effective-date: "2026-01-15"
        summary: "Submission packages must include parcel geometry summary and traceable compliance findings."
      - id: "RLMUA-2025-11"
        title: "Minimum frontage clarification"
        clause-reference: "RLMUA-LS 5.3"
        effective-date: "2025-11-01"
        summary: "Irregular parcel designs require documented frontage and access rationale."
      - id: "NMP-2025-09"
        title: "Master plan boundary submission alignment"
        clause-reference: "NMP-CAD 3.2"
        effective-date: "2025-09-20"
        summary: "Submissions outside approved master-plan extents are automatically escalated for review."
  ai:
    default-optimization-mode: BALANCED
  metrics:
    storage-capacity-mb: 10240
  oauth:
    google-client-id: ${GOOGLE_CLIENT_ID:}
  mail:
    from-address: ${GEOSMART_MAIL_USERNAME:badagaclass@gmail.com}
  demo:
    default-test-password: ${DEFAULT_TEST_PASSWORD:GeoSmart@2026}
